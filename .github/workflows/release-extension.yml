# Temporarily neutered to clear a wedged queued workflow_dispatch run (#1).
# The full workflow will be restored once GitHub abandons the stuck run.
# See ai_conversations/2026-05-23-release-stuck-and-ai-help-in-toolbar.md
name: Release Parish Trainer extension (disabled — clearing stuck run)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow is temporarily disabled while clearing a stuck queue."
